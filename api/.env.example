# Where the API listens
PORT=3001

# CORS allow-list (frontend dev origin)
FRONTEND_ORIGIN=http://localhost:3000

# Override path resolution if running outside the monorepo
# PROBLEMS_DIR=/absolute/path/to/problems
# CCACHE_DIR=/absolute/path/to/.ccache
# BUILD_ROOT=/absolute/path/to/.builds
