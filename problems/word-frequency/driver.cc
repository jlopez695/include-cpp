#include <iostream>
#include <string>
#include <map>

#include "utilities.hpp"

int main() {
  std::string str = "Howdy, Ags! Whoop!";

  // std::map<std::string,unsigned int> result = WordFrequencyCounter(str);

  // for (auto const& [key, value] : result) {
  //   std::cout << key    << ':'    << value    << std::endl;
  // }

}
